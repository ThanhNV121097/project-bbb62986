# DATABASE_URL is full PostgreSQL connection URL injected by runtime.
DATABASE_URL=postgres://app:app_secret@localhost:5432/app?sslmode=disable

# PORT is HTTP listen port. Runtime injects it; app falls back to APP_PORT then 8080.
PORT=8080

# APP_PORT is optional fallback HTTP listen port.
APP_PORT=8080
